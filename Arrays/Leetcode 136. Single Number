class Solution {
public:
    int singleNumber(vector<int>& nums) {
        int n= nums.size();
        int var=0;
        for(int i=0;i<n;i++)
        {
            var=var^nums[i];
        }
        return var;
    }
};
